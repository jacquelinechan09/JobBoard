<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Result</title>
</head>
<body>
    <%- include('../../../ejs-components/header.ejs') %>
    <p>Employees with average rating higher than <%= locals.input %>:</p>
    <table border="1px">
        <tr>
            <th>Email</th>
            <th>Average Rating</th>
        </tr>
        <% locals.data.forEach(row => { %>
        <tr>
            <td><%= row.email %></td>
            <td><%= parseFloat(row.avg).toFixed(3) %></td>
        </tr>
        <% }) %>
    </table>
    <p><a href="/employee/hiringManager/home">Go back</a></p>
</body>
</html>
